@model IEnumerable<Tutorias.Models.ContactType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Contact_Type)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Contact_Type)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContactTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContactTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContactTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
